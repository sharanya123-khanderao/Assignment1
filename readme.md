This Assignment enriches a CSV of city expenses with geolocation, weather, and FX rate data and saves the results as JSON.

## How to Run and Test
- pip install -r requirements.txt
- Make sure input csv file is present
- Run the program '''python main.py'''
- Check the generated json file output

